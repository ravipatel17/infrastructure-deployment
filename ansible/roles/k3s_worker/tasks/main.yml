- name: Install K3s agent
  shell: |
    curl -sfL https://get.k3s.io | K3S_URL="https://{{ hostvars['k3s-node-1'].master_ip }}:6443" K3S_TOKEN="{{ hostvars['k3s-node-1'].join_token }}" sh -
  args:
    creates: /usr/local/bin/k3s-agent
